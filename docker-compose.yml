version: '3.8'

services:
  mcp-ebook-server:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ${EBOOK_LIBRARY_PATH:-./ebooks}:/ebooks:ro
    environment:
      - EBOOK_LIBRARY_PATH=/ebooks
      - MCP_HOST=0.0.0.0
      - MCP_PORT=8080
    restart: unless-stopped

